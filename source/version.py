# set version info (for logging)
version_number = "0.0.1"
version_info = "initial prototype"

# set app info (for swagger)
app_title = f"finance backend"
app_description = f"{app_title} v{version_number} by coolseaweeed"
